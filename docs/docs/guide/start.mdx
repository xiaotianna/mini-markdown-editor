import { PackageManagerTabs } from '@rspress/theme-default';

# 快速开始

## 在线尝试

可以直接在 [在线尝试](../example) 上进行在线尝试。

## 安装

<PackageManagerTabs command="install @mini-markdown-rc/editor" />

## 使用

```tsx title="App.tsx"
import { Editor } from "@mini-markdown-rc/editor";

export default function App() {
  return <Editor />;
}
```
