import type { ViewUpdate as _ViewUpdate, EditorView as _EditorView } from "@uiw/react-codemirror";

// 导出 CodeMirror 的 ts 类型，暴露给外部使用
export type EditorView = _EditorView;
export type ViewUpdate = _ViewUpdate;
